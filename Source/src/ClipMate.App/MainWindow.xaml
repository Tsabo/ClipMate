<dx:ThemedWindow x:Class="ClipMate.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
        xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
        xmlns:views="clr-namespace:ClipMate.App.Views"
        xmlns:controls="clr-namespace:ClipMate.App.Controls"
        xmlns:converters="clr-namespace:ClipMate.App.Converters"
        mc:Ignorable="d"
        Title="ClipMate - Clipboard Manager"
        WindowStartupLocation="CenterScreen"
        Height="700"
        Width="1400"
        MinHeight="500"
        MinWidth="1000"
        ShowInTaskbar="True"
        StateChanged="Window_StateChanged">

    <dx:ThemedWindow.Resources>
        <!-- Converters -->
        <dxmvvm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </dx:ThemedWindow.Resources>

    <dx:ThemedWindow.InputBindings>
        <KeyBinding Key="T" Modifiers="Control+Shift" Command="{Binding ManageTemplatesCommand}" />
    </dx:ThemedWindow.InputBindings>

    <DockPanel>
        <!-- BarManager: Toolbar and Menus -->
        <dxb:BarManager x:Name="BarManager" DockPanel.Dock="Top">
            <dxb:BarManager.Bars>
                <!-- Main Toolbar -->
                <dxb:Bar Caption="Main Toolbar" IsMainMenu="False" AllowCollapse="True">
                    <!-- PowerPaste Button with state indicator -->
                    <dxb:BarButtonItem x:Name="PowerPasteButton"
                                       Content="PowerPaste"
                                       Command="{Binding PowerPasteToggleCommand}"
                                       ToolTip="Click to start PowerPaste. Click again to change direction or stop."
                                       LargeGlyph="{dx:DXImage 'SvgImages/Actions/Forward.svg'}"
                                       Glyph="{dx:DXImage 'SvgImages/Actions/Forward.svg'}" />
                </dxb:Bar>
                
                <!-- Menu Bar -->
                <dxb:Bar Caption="Main Menu" IsMainMenu="True">
                    <dxb:BarSubItem Content="_File">
                        <dxb:BarButtonItem Content="_Exit" ItemClick="Exit_Click" />
                    </dxb:BarSubItem>
                    
                    <dxb:BarSubItem Content="_Edit">
                        <dxb:BarButtonItem Content="PowerPaste _Up"
                                          Command="{Binding PowerPasteUpCommand}"
                                          KeyGesture="Ctrl+Up" />
                        <dxb:BarButtonItem Content="PowerPaste _Down"
                                          Command="{Binding PowerPasteDownCommand}"
                                          KeyGesture="Ctrl+Down" />
                        <dxb:BarItemSeparator />
                        <dxb:BarCheckItem Content="Explode Into _Fragments"
                                         IsChecked="{Binding IsExplodeMode}"
                                         ToolTip="Break clips into fragments by delimiters" />
                        <dxb:BarCheckItem Content="PowerPaste _Loop"
                                         IsChecked="{Binding IsLoopMode}"
                                         ToolTip="Loop back to start when reaching the end" />
                    </dxb:BarSubItem>
                    
                    <dxb:BarSubItem Content="_View">
                        <dxb:BarCheckItem Content="_Dual ClipList Mode"
                                         IsChecked="{Binding IsDualClipListMode}"
                                         KeyGesture="Ctrl+D" />
                    </dxb:BarSubItem>
                    
                    <dxb:BarSubItem Content="_Tools">
                        <dxb:BarButtonItem Content="_Text Tools" ItemClick="TextTools_Click" KeyGesture="Ctrl+T" />
                        <dxb:BarItemSeparator />
                        <dxb:BarButtonItem Content="_Options ..." ItemClick="Options_Click" />
                    </dxb:BarSubItem>
                    
                    <dxb:BarSubItem Content="T_emplates">
                        <dxb:BarButtonItem Content="_Manage Templates..." ItemClick="ManageTemplates_Click" KeyGesture="Ctrl+Shift+T" />
                    </dxb:BarSubItem>
                </dxb:Bar>
            </dxb:BarManager.Bars>
        </dxb:BarManager>

        <!-- Status Bar at Bottom -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusBarLeft" Text="Ready" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="StatusBarRight" Text="0 clips | 0 bytes, 0 chars, 0 words" />
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content Area -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="150" MaxWidth="400" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" MinWidth="400" />
            </Grid.ColumnDefinitions>

            <!-- Left Pane: Collection Tree -->
            <GroupBox Grid.Column="0" Header="Collections">
                <views:CollectionTreeView x:Name="CollectionTree"
                                          DataContext="{Binding CollectionTree}" />
            </GroupBox>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          Background="Transparent" />

            <!-- Center: ClipList Component(s) and ClipViewer -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <!-- ClipList area (dual mode support) -->
                    <RowDefinition Height="*" MinHeight="200" />
                    <!-- Splitter between ClipList and ClipViewer -->
                    <RowDefinition Height="5" />
                    <!-- ClipViewer area -->
                    <RowDefinition Height="*" MinHeight="200" />
                </Grid.RowDefinitions>

                <!-- ClipList area with dual mode support -->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <!-- Primary ClipList - always visible -->
                        <RowDefinition Height="*" />
                        <!-- Splitter - visible only in dual mode -->
                        <RowDefinition Height="Auto" />
                        <!-- Secondary ClipList - visible only in dual mode -->
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Primary ClipList -->
                    <GroupBox Grid.Row="0" Header="Clips (Primary)">
                        <views:ClipListView x:Name="ClipListView"
                                            Items="{Binding PrimaryClipList.Clips, Mode=OneWay}"
                                            SelectedItem="{Binding PrimaryClipList.SelectedClip}"
                                            SelectedItems="{Binding PrimaryClipList.SelectedClips, Mode=TwoWay}" />
                    </GroupBox>

                    <!-- Splitter for dual ClipList mode -->
                    <GridSplitter Grid.Row="1"
                                  Height="5"
                                  HorizontalAlignment="Stretch"
                                  Background="Transparent"
                                  Visibility="{Binding IsDualClipListMode, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <!-- Secondary ClipList (TODO: Low priority - needs keyed services) -->
                    <!--<GroupBox Grid.Row="2" 
                             Header="Clips (Secondary - Click to Change)"
                             Height="350"
                             Visibility="{Binding IsDualClipListMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <views:ClipListView x:Name="SecondaryClipListView"
                                           Items="{Binding SecondaryClips}"
                                           SelectionChanged="SecondaryClipListView_SelectionChanged"/>
                    </GroupBox>-->
                </Grid>

                <!-- Vertical Splitter between ClipList and ClipViewer -->
                <GridSplitter Grid.Row="1"
                              Height="5"
                              HorizontalAlignment="Stretch"
                              Background="Transparent" />

                <!-- ClipViewer area -->
                <controls:ClipViewerControl Grid.Row="2" x:Name="ClipViewer" />
            </Grid>
        </Grid>
    </DockPanel>
</dx:ThemedWindow>
