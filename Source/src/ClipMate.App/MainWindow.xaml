<Window x:Class="ClipMate.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClipMate.App"
        xmlns:vm="clr-namespace:ClipMate.App.ViewModels"
        xmlns:views="clr-namespace:ClipMate.App.Views"
        mc:Ignorable="d"
        Title="ClipMate - Clipboard Manager" 
        Height="700" 
        Width="1200"
        MinHeight="500"
        MinWidth="800">
    
    <Window.InputBindings>
        <KeyBinding Key="T" Modifiers="Control+Shift" Command="{Binding ManageTemplatesCommand}"/>
    </Window.InputBindings>
    
    <Window.Resources>
        <!-- Common Styles -->
        <Style x:Key="PaneHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5,5,5,10"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style x:Key="PaneBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>

    <!-- Main Grid with menu bar, toolbar, and content -->
    <Grid>
        <Grid.RowDefinitions>
            <!-- Menu Bar Row -->
            <RowDefinition Height="Auto"/>
            <!-- Toolbar Row -->
            <RowDefinition Height="Auto"/>
            <!-- Main Content Row -->
            <RowDefinition Height="*"/>
            <!-- Status Bar Row -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Menu Bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New Collection..." />
                <MenuItem Header="_Open Collection..." />
                <Separator/>
                <MenuItem Header="_Exit" Click="Exit_Click" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Copy" InputGestureText="Ctrl+C"/>
                <MenuItem Header="_Paste" InputGestureText="Ctrl+V"/>
                <MenuItem Header="_Delete" InputGestureText="Del"/>
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="_Text Tools" Click="TextTools_Click" InputGestureText="Ctrl+T"/>
                <MenuItem Header="_PowerPaste" InputGestureText="Ctrl+Shift+V"/>
            </MenuItem>
            <MenuItem Header="T_emplates">
                <MenuItem Header="_Manage Templates..." Click="ManageTemplates_Click" InputGestureText="Ctrl+Shift+T"/>
                <Separator/>
                <MenuItem Header="_Insert Template" x:Name="InsertTemplateMenu">
                    <!-- Template items will be populated dynamically -->
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About ClipMate" />
            </MenuItem>
        </Menu>
        
        <!-- Toolbar (Placeholder - will add icons in next task) -->
        <ToolBarTray Grid.Row="1" Background="#F0F0F0">
            <ToolBar>
                <Button Content="New" ToolTip="New Collection" Padding="5"/>
                <Button Content="Folder" ToolTip="New Folder" Padding="5"/>
                <Separator/>
                <Button Content="Copy" ToolTip="Copy to Clipboard" Padding="5"/>
                <Button Content="Paste" ToolTip="Paste" Padding="5"/>
                <Button Content="Delete" ToolTip="Delete Clip" Padding="5"/>
                <Separator/>
                <Button Content="Search" ToolTip="Search Clips" Padding="5"/>
            </ToolBar>
        </ToolBarTray>
        
        <!-- ClipMate 7.5 Style Layout: Left Tree + Right (Top List + Bottom Preview) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <!-- Left Pane: Collections & Folders Tree -->
                <ColumnDefinition Width="250" MinWidth="150" MaxWidth="400"/>
                <ColumnDefinition Width="Auto"/>
                <!-- Right Pane: Clip List (top) + Preview (bottom) -->
                <ColumnDefinition Width="*" MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <!-- Left Pane: Collections & Folders -->
            <Border Grid.Column="0" Style="{StaticResource PaneBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                              Text="Collection Tree" 
                              Style="{StaticResource PaneHeaderStyle}"/>
                    
                    <views:CollectionTreeView Grid.Row="1" x:Name="CollectionTree"/>
                </Grid>
            </Border>

            <!-- Splitter between Left and Right -->
            <GridSplitter Grid.Column="1" 
                         Width="5" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Stretch"
                         Background="#E0E0E0"
                         ShowsPreview="True"/>

            <!-- Right Side: Clip List (Top) + Preview Pane (Bottom) -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <!-- Clip List Area -->
                    <RowDefinition Height="*" MinHeight="200"/>
                    <RowDefinition Height="Auto"/>
                    <!-- Preview Area -->
                    <RowDefinition Height="300" MinHeight="150"/>
                </Grid.RowDefinitions>

                <!-- Clip List Section -->
                <Border Grid.Row="0" Style="{StaticResource PaneBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Search Panel (moved from top of window) -->
                        <views:SearchPanel Grid.Row="0" x:Name="SearchPanel" Margin="0,0,0,5"/>

                        <!-- Clip List - DataGrid with sortable columns (ClipMate 7.5 style) -->
                        <DataGrid Grid.Row="1" 
                                 x:Name="ClipDataGrid"
                                 ItemsSource="{Binding Clips}"
                                 AutoGenerateColumns="False"
                                 SelectionMode="Single"
                                 CanUserAddRows="False"
                                 CanUserDeleteRows="False"
                                 GridLinesVisibility="Horizontal"
                                 HeadersVisibility="Column"
                                 RowHeight="24"
                                 BorderThickness="0"
                                 Background="White"
                                 AlternatingRowBackground="#F9F9F9"
                                 SelectionChanged="ClipDataGrid_SelectionChanged">
                            <DataGrid.Columns>
                                <!-- Title Column (Clip Content Preview) -->
                                <DataGridTextColumn Header="Title" 
                                                   Binding="{Binding DisplayTitle}" 
                                                   Width="*" 
                                                   CanUserSort="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding TextContent}"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <!-- SortKey Column -->
                                <DataGridTextColumn Header="SortKey" 
                                                   Binding="{Binding Id}" 
                                                   Width="80" 
                                                   CanUserSort="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Foreground" Value="#666666"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                
                                <!-- Date/Time Column -->
                                <DataGridTextColumn Header="Date/Time" 
                                                   Binding="{Binding CapturedAt, StringFormat='{}{0:M/d/yyyy h:mm tt}'}" 
                                                   Width="140" 
                                                   CanUserSort="True"
                                                   SortDirection="Descending">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#666666"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Splitter between List and Preview -->
                <GridSplitter Grid.Row="1" 
                             Height="5" 
                             HorizontalAlignment="Stretch" 
                             VerticalAlignment="Center"
                             Background="#E0E0E0"
                             ShowsPreview="True"/>

                <!-- Preview Pane Section (Bottom) -->
                <Border Grid.Row="2" Style="{StaticResource PaneBorderStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Preview Header with Tabs -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0"
                                      Text="Preview" 
                                      Style="{StaticResource PaneHeaderStyle}"/>
                            
                            <!-- Preview Format Tabs -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,5">
                                <Button Content="Text" Width="60" Height="25" Margin="2,0" x:Name="TextTabButton" Click="PreviewTab_Click" Tag="Text"/>
                                <Button Content="Rich Text" Width="70" Height="25" Margin="2,0" x:Name="RichTextTabButton" Click="PreviewTab_Click" Tag="RichText"/>
                                <Button Content="HTML" Width="60" Height="25" Margin="2,0" x:Name="HtmlTabButton" Click="PreviewTab_Click" Tag="Html"/>
                            </StackPanel>
                        </Grid>

                        <!-- Preview Content Area -->
                        <ScrollViewer Grid.Row="1" 
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     Background="White">
                            <TextBlock x:Name="PreviewTextBlock"
                                      Text="Select a clip to preview..."
                                      TextWrapping="Wrap"
                                      Foreground="Gray"
                                      FontStyle="Italic"
                                      Margin="10"
                                      FontFamily="Consolas"/>
                        </ScrollViewer>

                        <!-- Preview Actions -->
                        <StackPanel Grid.Row="2" 
                                   Orientation="Horizontal" 
                                   HorizontalAlignment="Right"
                                   Margin="0,5,0,0">
                            <Button Content="Copy" 
                                   x:Name="CopyButton"
                                   Width="80" 
                                   Height="28" 
                                   Margin="5,0"
                                   Click="CopyButton_Click"/>
                            <Button Content="Delete" 
                                   x:Name="DeleteButton"
                                   Width="80" 
                                   Height="28" 
                                   Margin="5,0"
                                   Background="#FFEBEE"
                                   Foreground="#C62828"
                                   Click="DeleteButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="3" 
               x:Name="StatusBar"
               BorderBrush="#CCCCCC" 
               BorderThickness="0,1,0,0" 
               Padding="5"
               Background="#F0F0F0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                          x:Name="StatusBarLeft"
                          Text="Ready" 
                          FontSize="11" 
                          Foreground="#666666"
                          Margin="0,0,20,0"/>
                
                <TextBlock Grid.Column="2" 
                          x:Name="StatusBarRight"
                          Text="0 clips | 0 bytes, 0 chars, 0 words" 
                          FontSize="11" 
                          Foreground="#666666"/>
            </Grid>
        </Border>
    </Grid>
</Window>