<ui:FluentWindow
    x:Class="ClipMate.App.Views.PowerPasteWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:ClipMate.App.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:PowerPasteViewModel}"
    mc:Ignorable="d"
    Title="PowerPaste"
    Width="600"
    Height="400"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    Loaded="Window_Loaded"
    PreviewKeyDown="Window_PreviewKeyDown">

    <ui:FluentWindow.Resources>
        <!--  Clip Item Style  -->
        <Style x:Key="ClipItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,0,0,2" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>

    <Border
        Background="#1E1E1E"
        BorderBrush="#3F3F46"
        BorderThickness="1"
        CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="20"
                Opacity="0.5"
                ShadowDepth="0"
                Color="Black" />
        </Border.Effect>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Title  -->
            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,12"
                FontWeight="SemiBold"
                Text="PowerPaste" />

            <!--  Search Box  -->
            <TextBox
                x:Name="SearchTextBox"
                Grid.Row="1"
                Height="36"
                Padding="12,8"
                BorderThickness="1"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4" />
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!--  Clip List  -->
            <ListBox
                x:Name="ClipList"
                Grid.Row="2"
                Margin="0,12,0,0"
                BorderThickness="0"
                ItemContainerStyle="{StaticResource ClipItemStyle}"
                ItemsSource="{Binding FilteredClips}"
                SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!--  Clip Content  -->
                            <TextBlock
                                Grid.Column="0"
                                Text="{Binding TextContent}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap"
                                ToolTip="{Binding TextContent}" />

                            <!--  Source Application  -->
                            <TextBlock
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                Text="{Binding SourceApplicationName}"
                                TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!--  Footer Hints  -->
            <Grid Grid.Row="3" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="0"
                    Text="↑↓ Navigate  •  Enter to Paste  •  Esc to Cancel" />

                <TextBlock
                    Grid.Column="1">
                    <Run Text="Showing" />
                    <Run Text="{Binding FilteredClips.Count, Mode=OneWay}" />
                    <Run Text="of" />
                    <Run Text="{Binding Clips.Count, Mode=OneWay}" />
                    <Run Text="clips" />
                </TextBlock>
            </Grid>
        </Grid>
    </Border>
</ui:FluentWindow>
