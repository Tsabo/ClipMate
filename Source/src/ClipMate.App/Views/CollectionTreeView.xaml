<UserControl x:Class="ClipMate.App.Views.CollectionTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="250">
    <UserControl.Resources>
        <!-- Styles are handled in cell template directly -->
    </UserControl.Resources>

    <!-- DevExpress TreeViewControl for hierarchical collection tree -->
    <dxg:TreeViewControl x:Name="TreeView"
                         ItemsSource="{Binding RootNodes}"
                         TreeViewFieldName="Name"
                         ImageFieldName="IconImage"
                         ChildNodesPath="Children"
                         ShowBorder="True"
                         TreeLineStyle="Solid"
                         SelectedItem="{Binding SelectedNode, Mode=TwoWay}" ShowNodeImages="True">

        <!-- Context Menu -->
        <dxg:TreeViewControl.ContextMenu>
            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                <!-- Move Up/Down -->
                <MenuItem Header="Move Up In Sort Order" InputGestureText="(+)">
                    <MenuItem.Icon>
                        <emoji:TextBlock Text="⬆️" FontSize="14" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Move Down In Sort Order" InputGestureText="(-)">
                    <MenuItem.Icon>
                        <emoji:TextBlock Text="⬇️" FontSize="14" />
                    </MenuItem.Icon>
                </MenuItem>

                <!-- Add New Collection -->
                <MenuItem Header="Add New Collection">
                    <MenuItem.Icon>
                        <emoji:TextBlock Text="📁" FontSize="14" />
                    </MenuItem.Icon>
                </MenuItem>

                <!-- Delete Collection -->
                <MenuItem Header="Delete Collection" />

                <!-- Database Operations -->
                <MenuItem Header="Activate Database" />
                <MenuItem Header="De-Activate Database" />

                <!-- Tree Operations -->
                <MenuItem Header="Expand All" />
                <MenuItem Header="Collapse All" />
                <MenuItem Header="Show All Clips In All Children" />

                <MenuItem Header="Cancel" />

                <Separator />

                <!-- Properties -->
                <MenuItem Header="Properties"
                          Command="{Binding ShowPropertiesCommand}">
                    <MenuItem.Icon>
                        <emoji:TextBlock Text="📁" FontSize="14" />
                    </MenuItem.Icon>
                </MenuItem>
            </ContextMenu>
        </dxg:TreeViewControl.ContextMenu>
    </dxg:TreeViewControl>
</UserControl>
