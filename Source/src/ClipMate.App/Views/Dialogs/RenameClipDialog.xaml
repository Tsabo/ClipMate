<dx:ThemedWindow x:Class="ClipMate.App.Views.Dialogs.RenameClipDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                 xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                 mc:Ignorable="d"
                 Title="Rename Clip"
                 Width="500"
                 Height="280"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False"
                 SizeToContent="Height">

    <!-- DevExpress Dialog Buttons -->
    <dx:ThemedWindow.DialogButtons>
        <dx:ThemedWindowDialogButton Content="OK"
                                     Command="{Binding OkCommand}"
                                     IsDefault="True"
                                     DialogResult="OK"
                                     MinWidth="80" />
        <dx:ThemedWindowDialogButton Content="Cancel"
                                     IsCancel="True"
                                     DialogResult="Cancel"
                                     MinWidth="80" />
        <dx:ThemedWindowDialogButton Content="Help"
                                     Command="{Binding HelpCommand}"
                                     DialogResult="None"
                                     MinWidth="80" />
    </dx:ThemedWindow.DialogButtons>

    <dxlc:LayoutControl Orientation="Vertical">
        <dxlc:LayoutGroup Orientation="Vertical">
            <dxlc:LayoutItem Label="Title" LabelPosition="Top">
                <dxe:TextEdit x:Name="TitleTextBox"
                              Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                              Height="32"
                              FontSize="13" />
            </dxlc:LayoutItem>
            <dxlc:LayoutItem Label="ShortCut" LabelPosition="Top">
                <dxe:TextEdit x:Name="ShortCutTextBox"
                              Text="{Binding ShortCut, UpdateSourceTrigger=PropertyChanged}"
                              Height="32"
                              FontSize="13"
                              MaxLength="64" />
            </dxlc:LayoutItem>
            <dxlc:LayoutItem>
                <dxe:CheckEdit Content="Carry Over Last Prefix"
                               IsChecked="{Binding CarryOverLastPrefix}"
                               FontSize="13"
                               Margin="0,5,0,0" />
            </dxlc:LayoutItem>
        </dxlc:LayoutGroup>
    </dxlc:LayoutControl>
</dx:ThemedWindow>
