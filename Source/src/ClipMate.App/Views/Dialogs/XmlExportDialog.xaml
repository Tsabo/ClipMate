<dx:ThemedWindow x:Class="ClipMate.App.Views.Dialogs.XmlExportDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                 xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 Title="Export to XML"
                 Width="700"
                 Height="325"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 SizeToContent="Height"
                 ShowInTaskbar="False">

    <!-- DevExpress Dialog Buttons -->
    <dx:ThemedWindow.DialogButtons>
        <dx:ThemedWindowDialogButton Content="Go"
                                     Command="{Binding ExecuteXmlExportCommand}"
                                     IsDefault="True"
                                     AllowCloseWindow="False" />
        <dx:ThemedWindowDialogButton Content="Abort"
                                     IsCancel="True"
                                     Command="{Binding AbortCommand}" />
        <dx:ThemedWindowDialogButton Content="Done"
                                     Command="{Binding DoneCommand}" />
    </dx:ThemedWindow.DialogButtons>

    <dxlc:LayoutControl Orientation="Vertical" Padding="16">
        <dxlc:LayoutGroup Header="XML Export" Orientation="Vertical">
            <dxlc:LayoutGroup Orientation="Horizontal">
                <dxlc:LayoutItem Label="File Path" HorizontalAlignment="Stretch">
                    <dxe:TextEdit Text="{Binding XmlExportFilePath, Mode=TwoWay}" />
                </dxlc:LayoutItem>
                <dxlc:LayoutItem HorizontalAlignment="Right">
                    <dx:SimpleButton Content="Browse..." Margin="8,0,0,0" Click="BrowseXmlExportFile_Click" />
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
            <dxlc:LayoutItem>
                <CheckBox Content="Include collections" IsChecked="{Binding XmlExportIncludeCollections, Mode=TwoWay}" />
            </dxlc:LayoutItem>
        </dxlc:LayoutGroup>

        <dxlc:LayoutGroup Header="Processing" Orientation="Vertical">
            <TextBox Height="180" Text="{Binding ProcessingLog}" IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" />
        </dxlc:LayoutGroup>

        <dxlc:LayoutGroup>
            <dxlc:LayoutItem>
                <dxe:ProgressBarEdit Value="{Binding ProgressValue}"
                                     Maximum="{Binding ProgressMaximum}"
                                     ContentDisplayMode="None" />
            </dxlc:LayoutItem>
        </dxlc:LayoutGroup>
    </dxlc:LayoutControl>
</dx:ThemedWindow>
